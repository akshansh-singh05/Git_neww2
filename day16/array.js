// linear collection of things

let arr = ["mango", "hello", "gauvava"];

console.log(arr.length);
console.log(arr);

// we can also create mixed type of array... in JS while in java we can only create a single type of array


// to create empty array
let emparr = [];

console.log(emparr); 
console.log(emparr.length); 

   // strings are immutable while arrays are emmutable....
   // it means we can change value of original array by changing in the array

   arr[10] = "papaya";

console.log(arr); 
console.log(arr.length); 
