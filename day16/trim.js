let msg = "   Babuu   ";

let v = msg.trim();

console.log(v);

// trim is used to remove extra spaces from the string

// str.trim() do not change the original string but makes a new string

    // Immutable strings // 
// in JS no changes in the string can be made 

// whenever we do try to make a change, a new string is created and old one remains the same "as in the string trim method"

