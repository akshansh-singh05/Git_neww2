// Argument is the some value that we pass to the method

// format == stringNAme.method(arg);
                // in arg we can pass multiple arguments

                /*
                methods are of different type for example 
                
                let str = "Ilovecoding"

                str.indexOf("love")  // 1
                str.indexOf("love")  // -1 Not found
                str.indexOf("love")  // 2 (only one index)
        
                */


let str = "     My name is akshansh       ";

let strn = str.indexOf("a");

console.log(strn);

// method chaininggg    

let abc = str.toUpperCase().trim(); // it compute from left to right

console.log(abc);
